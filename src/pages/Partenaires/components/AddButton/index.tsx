import React, { FC } from "react";

export const AddButton: FC = () => {
  return (
    <button className="ADD_BUTTON">
      <i className="fas fa-plus"></i> Ajouter un partenaire
    </button>
  );
};
